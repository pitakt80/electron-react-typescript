import * as React from 'react';

import CounterContainer from '../containers/CounterContainer';
import PinKeyContainer from '../containers/PinKeyContainer';

const Application = () => (
    /*
    <div>
        Hello World from Electron!
        <CounterContainer />
        <PinKeyContainer /> 
    </div>
*/
    <div>
        <PinKeyContainer />
    </div>
);

export default Application;
